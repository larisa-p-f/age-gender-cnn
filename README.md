# age-gender-cnn
Convolutional Neural Network project for predicting age (regression) and gender (classification) from facial images, using both a custom CNN and transfer learning with VGG16. Achieved ~6.6 MAE for age and ~88% accuracy for gender
